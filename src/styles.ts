import {css} from "@emotion/core";

export const styles = () => {
    return css`
      :root {
        --sidebar-width: 325px;
      }
      
      body {
        background: #524573 url("/images/bg.png");
        background-size: 100px;
      }
`;
}